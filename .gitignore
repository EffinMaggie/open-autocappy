# don't commit node modules installed locally.
# npm can reconstruct them using package.json
node_modules/

# don't commit JS files - they're all generated by TypeScript.
*.js

# explicitly allow CommonJS files - they're written by hand.
!*.cjs

# don't commit doxygen output - it's better when it's fresh!
documentation/html

# don't commit doxyfile - the build script creates its own.
doxyfile
